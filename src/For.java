public class For {
    public static void main(String[] args) {

        // Infinity Loop
            // for(;;) {
            //     System.out.println("ini adalah contoh infinity loop!");
            // }


        // Contoh perulangan kedua: (hanya menggunakan kondisi)
            // var counter = 1;

            // for(; counter <= 10 ; ) {
            //     System.out.println("Ini adalah perulangan ke - " + counter);
            //     counter++;
            // }

        // Contoh perulangan kedua: (hanya menggunakan init statement & kondisi)
            // for (var counter = 1; counter <= 10 ; ){
            //     System.out.println("Ini adalah perulangan ke - " + counter);

            //     counter++;
            // }

        // Contoh perulangan kedua: (hanya menggunakan init statement, kondisi, & post statement)
            for (var counter = 1; counter <= 10 ; counter++){
                System.out.println("Ini adalah perulangan ke - " + counter);
            }
        
    }
}
